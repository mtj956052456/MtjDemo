<?xml version="1.0" encoding="utf-8"?>
<litepal>
    <!--定义应用程序的数据库名称。默认情况下，每个数据库名称应以.db结尾。
    如果您没有使用.db命名数据库端，LitePal会自动为您添加后缀。-->
    <dbname value="demo" />

    <!--定义数据库的版本。每次要升级数据库时，版本标记都会有所帮助。
    修改您在映射标记中定义的模型，只需将版本值加1，即可自动处理数据库升级而无需担心。-->
    <version value="1" />

    <!--使用映射标记在列表中定义模型，LitePal将为每个映射类创建表。-->
    <list>
        <mapping class="com.example.pmprogect.fragment.home.litepal.News" />
    </list>

</litepal>